# Core daemon, control panel, REPL channel and the git/docker/code/file/network
# plugins run on the standard library alone. Everything below is optional and
# only needed for the feature named next to it.

# Agent SDK — primary engine for Anthropic models (falls back to the raw-urllib
# provider path in engine/providers.py when this is not installed).
claude-agent-sdk>=0.1.0

# Channels
requests>=2.31.0            # channels/whatsapp.py talks to the Node bridge
python-telegram-bot>=20.0   # telegram channel
qrcode[pil]>=7.4            # WhatsApp QR popup (pybridge/qr_popup.py)

# Plugins
psutil>=5.9.0               # plugins/process_monitor.py
playwright>=1.40.0          # plugins/browser.py  (also needs: playwright install chromium)
pip-audit>=2.6.0            # plugins/packages.py  "pip audit"

# Screen tools + tray app
Pillow>=10.0.0              # screenshots on Windows, tray icon
pystray>=0.19.0             # pybridge_tray.py
